# المهمة 6 — فحص أمني شامل لميزة الفحص الذكي للإيصالات (Smart Receipt Scan)

> ملاحظات مرجعية — تُكمّل ما بدأه الوكيل السابق (الذي توقّف بسبب نفاد الرصيد، لا لانتهاء المهمة).

## 1) بنية الميزة كما هي فعلاً (تم التحقق منها)

المسار كامل صار **خادمياً بالكامل**، والمتصفح لا يملك أي سلطة قرار مالي:

1. **المتصفح** (`assets/js/receipt-pipeline.js`): يضغط الصورة فقط ويرسلها إلى Edge Function
   `process-receipt`. لا يحتوي OCR ولا منطق قبول/رفض. يرفض المتابعة إن لم يُرجِع الخادم
   `scanId` + `receiptHash` (`server_scan_contract_invalid`).
2. **Edge Function** (`supabase/functions/process-receipt/index.ts`): يتحقق من الجلسة،
   يطبّق rate limit (12 فحص / 10 دقائق)، يحسب بصمة SHA-256 **على السيرفر** من بايتات
   الصورة، يشغّل Gemini OCR + التحكيم، ثم يخزّن النتيجة في `receipt_scan_results`
   بمفتاح service_role ويعيد `scanId`. `GEMINI_API_KEY` يُقرأ من `Deno.env` فقط.
3. **الواجهات** (`checkout.html`, `checkout-v2.html`, `wallet.html`): تمرّر
   `edge_scan_id = scanId` داخل `p_ocr_data` إلى الدوال:
   `create_bank_orders_bulk` / `create_wallet_topup_from_receipt` / `attach_secondary_receipt`.
4. **نقطة الاختناق الوحيدة** `claim_payment_receipt`: كل المسارات تمرّ عبرها، وهي التي
   تُعيد اشتقاق كل القيم الحاسمة من سجل الفحص الخادمي وتتجاهل ما يرسله المتصفح.

## 2) الثغرة الحرجة التي عالجتها المهمة

قبل الإصلاح كانت `claim_payment_receipt` تثق بـ `amount_detected` و`tx_ref_ocr` و`ocr_status`
القادمة من المتصفح، وملف "نقل الفحص إلى الخادم" لم يكن مطبَّقاً على القاعدة الحيّة إطلاقاً.
النتيجة: أي مستخدم مسجَّل يستطيع استدعاء الدالة عبر PostgREST بقيم مزوّرة ويحصل على طلب
`passed / amount_verified=true` بلا إيصال بنكي حقيقي.

**الإصلاح** (`supabase-SQL-المهمة-6-ربط-الفحص-الخادمي.sql`، idempotent):
- لا يُمنح وسم `passed/amount_verified` إلا بربط `edge_scan_id` بسجل فحص خادمي حقيقي
  يخص نفس المستخدم، غير منتهٍ، مسموح بإرساله، غير مرفوض.
- بصمة SHA-256 في سجل الفحص يجب أن تطابق بصمة الإيصال (`receipt_scan_hash_mismatch`).
- القيم الحاسمة تُؤخذ حصراً من سجل الفحص، وتُتجاهل قيم المتصفح.
- سجل الفحص يُستهلك مرة واحدة عبر `claimed_at` مع قفل `FOR UPDATE` (`receipt_scan_already_used`).
- عند غياب `edge_scan_id` كلياً (تعذّر الخادم): يُنشأ الطلب كـ `needs_review` فقط ولا يُمنح
  `passed` تلقائياً — يحفظ المسار المتدهور دون فتح ثغرة.

## 3) الباگ الذي أوقف الوكيل السابق (تم إصلاحه في هذا الملف)

اختبارات المسار الشرعي (T2/T3/T6) كانت تفشل بـ `column reference "id" is ambiguous`.
السبب: تعارض بين أعمدة `RETURNS TABLE(id uuid, ...)` (متغيرات OUT) وعمود `id` الفعلي في
`receipt_scan_results` داخل جملتَي `SELECT ... FOR UPDATE` و`UPDATE`، وكذلك عمود `id` في
`payment_receipts` داخل `RETURNING`.

**الحل المطبَّق في الملف:** إضافة alias صريح للجدول (`receipt_scan_results rs`) وتأهيل كل
مرجع: `rs.id`، و`RETURNING payment_receipts.id INTO v_id`. لا يوجد أي مرجع `id` غير مؤهَّل
متبقٍّ، ولا تعارض جديد على `ocr_status/amount_verified/ref_verified` (تظهر فقط في قائمة
أعمدة INSERT، وهي تُحسم للجدول دائماً).

## 4) نتيجة التدقيق الأمني (فحص كود ثابت — static review)

| بند اختبار القبول | الحالة | الدليل |
|---|---|---|
| القرار الحاسم يُعاد التحقق منه خادمياً ولا يُزوَّر من المتصفح | ✅ مؤكَّد كوداً | `claim_payment_receipt` تُعيد اشتقاق القيم من `receipt_scan_results` |
| منع إعادة استخدام نفس الإيصال (SHA-256) | ✅ مؤكَّد كوداً | تطابق البصمة + `claimed_at` أحادي الاستخدام |
| `GEMINI_API_KEY` في الأسرار وغير مكشوف بالعميل | ✅ مؤكَّد | يظهر فقط في `index.ts` عبر `Deno.env`؛ لا مرجع له في كود العميل |
| حماية دالة `process-receipt` (auth + rate limit) | ✅ مؤكَّد | `getUser(token)` + 12/10دقائق |
| كل خطأ مكتشف أُصلح ووُثّق | ✅ | باگ ambiguous id مُصلَح هنا وموثَّق أعلاه |

## 5) المتبقّي — تحقق زمن التشغيل (يحتاج اتصال قاعدة البيانات الحيّة)

هذان البندان لا يمكن إثباتهما بفحص الكود وحده؛ يحتاجان تنفيذاً فعلياً على القاعدة الحيّة:

1. **تطبيق `supabase-SQL-المهمة-6-ربط-الفحص-الخادمي.sql` على القاعدة الحيّة** (إن لم يكن
   نسخة الـ alias مطبَّقة بعد)، ثم **إعادة تشغيل T2/T3/T6** (معاملات rolled-back) للتأكد
   الفعلي أن `ambiguous id` اختفى وأن T1/T4/T5 لا تزال تمرّ.
2. **رفع إيصال حقيقي كامل من الواجهة end-to-end** (checkout + المحفظة) والتأكد أنه يصل
   لحالة مقبولة صحيحة.

> ملاحظة: تعذّر تنفيذ هذين البندين في هذه الجلسة لأن الاتصال المباشر بالقاعدة عبر
> `db.rglbfizqolrenwfsndyv.supabase.co` يحتاج IPv6 (غير متاح من بيئة التشغيل الحالية)،
> ولم يُفعَّل تكامل Supabase / السماح بتشغيل السكربتات في هذه الجلسة.

### ترتيب التطبيق المقترح في Supabase SQL Editor
1. `supabase-SQL-المهمة-6-ربط-الفحص-الخادمي.sql` (النسخة الحالية بالـ alias).
2. تشغيل اختبارات التلاعب داخل معاملة واحدة تنتهي بـ `ROLLBACK` (لا تلمس بيانات حقيقية).
