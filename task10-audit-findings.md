## CSS tokens/fonts
assets/css/style.css:5:  --bg: #ffffff;              /* الورقي الصافي — خلفية الموقع الأساسية */
assets/css/style.css:6:  --bg-card: #f5f1ea;         /* الورقي الخافت — خلفية الصناديق والحقول */
assets/css/style.css:7:  --primary: #ff6a1a;         /* البرتقالي الحيوي — العنصر التفاعلي الأساسي */
assets/css/style.css:8:  --primary-dim: #e85d00;     /* درجة برتقالية أغمق للتأثيرات والهوفر */
assets/css/style.css:34:  background: var(--bg);
assets/css/style.css:36:  font-family: var(--font-display);
assets/css/style.css:44:  font-family: var(--font-heading);
assets/css/style.css:57:button { font-family: inherit; cursor: pointer; border: none; }
assets/css/style.css:87:.logo span { color: var(--primary); }
assets/css/style.css:98:  background: var(--bg-card);
assets/css/style.css:157:  font-family: var(--font-mono);
assets/css/style.css:159:  color: var(--primary);
assets/css/style.css:190:  background: var(--bg-card);
assets/css/style.css:199:.dot.active { background: var(--primary); width: 18px; border-radius: 4px; }
assets/css/style.css:218:  font-family: var(--font-mono);
assets/css/style.css:220:  color: var(--primary);
assets/css/style.css:237:  background: var(--bg-card);
assets/css/style.css:246:  background: var(--primary);
assets/css/style.css:248:  border-color: var(--primary);
assets/css/style.css:267:  background: var(--bg-card);
assets/css/style.css:278:.voucher-card:hover { border-color: var(--primary); }
assets/css/style.css:291:  background: linear-gradient(135deg, #F5F1EA, #EDE6D9);
assets/css/style.css:326:  background: var(--bg);
assets/css/style.css:340:  font-family: var(--font-mono);
assets/css/style.css:342:  color: var(--primary);
assets/css/style.css:352:  background: var(--primary);
assets/css/style.css:380:  color: var(--primary);
assets/css/style.css:431:  background: var(--primary);
assets/css/style.css:456:  background: var(--bg-card);
assets/css/style.css:466:  background: linear-gradient(135deg, #F5F1EA, #EDE6D9);
assets/css/style.css:488:  font-family: var(--font-mono);
assets/css/style.css:489:  color: var(--primary);
assets/css/style.css:506:  background: var(--bg);
assets/css/style.css:508:  font-family: var(--font-display);
assets/css/style.css:517:  background: var(--bg);
assets/css/style.css:519:  font-family: var(--font-display);
assets/css/style.css:524:  border-color: var(--primary);
assets/css/style.css:536:  background: var(--bg);
assets/css/style.css:545:  background: var(--bg-card);
assets/css/style.css:546:  border-color: var(--primary);
assets/css/style.css:547:  color: var(--primary);
assets/css/style.css:563:  border-color: var(--primary);
assets/css/style.css:578:  color: var(--primary);
assets/css/style.css:584:.recharge-highlight-link:hover { color: var(--primary-dim); }
assets/css/style.css:587:  background: var(--bg);
assets/css/style.css:608:.file-upload.has-file { border-color: var(--primary); color: var(--primary); }
assets/css/style.css:611:  background: var(--bg-card);
assets/css/style.css:621:  font-family: var(--font-mono);
assets/css/style.css:623:  color: var(--primary);
assets/css/style.css:642:  background: var(--bg-card);
assets/css/style.css:661:  background: var(--bg);
assets/css/style.css:667:  color: var(--primary);
assets/css/style.css:715:  background: linear-gradient(135deg, #FFF3E8, var(--bg-card));
assets/css/style.css:727:  font-family: var(--font-mono);
assets/css/style.css:730:  color: var(--primary);
assets/css/style.css:742:  background: var(--primary);
assets/css/style.css:751:  background: var(--bg-card);
assets/css/style.css:760:.status-confirmed { background: rgba(255, 106, 26,0.15); color: var(--primary); }
assets/css/style.css:775:  background: var(--bg-card);
assets/css/style.css:807:  background: var(--bg-card);
assets/css/style.css:831:  background: linear-gradient(135deg, #FFF3E8, var(--bg));
assets/css/style.css:832:  border: 1px solid var(--primary);
assets/css/style.css:840:  font-family: var(--font-mono);
assets/css/style.css:843:  color: var(--primary);
assets/css/style.css:859:.drawer-nav a:active { background: var(--bg); }
assets/css/style.css:874:  background: var(--bg);
assets/css/style.css:885:  background: var(--bg-card);
assets/css/style.css:908:  color: var(--primary);
assets/css/style.css:938:  background: linear-gradient(135deg, #F5F1EA, #EDE6D9);
assets/css/style.css:971:  background: var(--bg-card);
assets/css/style.css:1001:  color: var(--primary);
assets/css/style.css:1002:  font-family: var(--font-mono);
assets/css/style.css:1007:  border-color: var(--primary);
assets/css/style.css:1018:  outline: 2px solid var(--primary);
assets/css/style.css:1048:  background: var(--primary);
assets/css/style.css:1106:  --primary-soft: rgba(255, 106, 26, .10);
assets/css/style.css:1107:  --primary-softer: rgba(255, 106, 26, .055);
assets/css/style.css:1108:  --surface-elevated: #fffdfa;
assets/css/style.css:1115:button, input, select, textarea { font-family: var(--font-display); }
assets/css/style.css:1137:.icon-btn:hover, .hamburger-btn:hover { transform: translateY(-1px); color: var(--primary); border-color: rgba(255,106,26,.40); box-shadow: var(--shadow-card); }
assets/css/style.css:1143:  background: var(--surface-elevated);
assets/css/style.css:1148:.back-link:hover { transform: translateY(-1px); color: var(--primary-dim); border-color: rgba(255,106,26,.42); box-shadow: var(--shadow-card); }
assets/css/style.css:1156:  border-color: var(--primary);
assets/css/style.css:1157:  background: var(--surface-elevated);
assets/css/style.css:1164:.payment-option:hover:not(.disabled) { transform: translateY(-1px); border-color: rgba(255,106,26,.48); box-shadow: var(--shadow-card), 0 0 0 3px var(--primary-softer); }
assets/css/style.css:1165:.payment-option.selected { box-shadow: 0 0 0 3px var(--primary-soft), var(--shadow-card); }
assets/css/style.css:1167:.file-upload:hover { transform: translateY(-1px); border-color: rgba(255,106,26,.55); background: var(--primary-softer); color: var(--primary-dim); }
assets/css/style.css:1168:.file-upload.has-file { background: var(--primary-softer); box-shadow: 0 0 0 3px var(--primary-soft); }
assets/css/style.css:1170:.copy-account-btn:hover { transform: translateY(-1px); border-color: rgba(255,106,26,.46); background: var(--primary-softer); }
assets/css/style.css:1173:.section-label::before { content: ''; width: 5px; height: 18px; border-radius: 99px; background: var(--primary); box-shadow: 0 4px 12px rgba(255,106,26,.28); }
assets/css/style.css:1174:.topup-toggle-btn { background: linear-gradient(135deg, #ff7a2b, var(--primary) 62%, var(--primary-dim)); color: var(--ink); }
assets/css/style.css:1197:  background: var(--bg-card);
assets/css/style.css:1205:  border-color: var(--primary);
assets/css/style.css:1219:  color: var(--primary);
assets/css/style.css:1250:.catalog-subcategory-info > i { color: var(--primary); }
assets/css/admin.css:22:  background: var(--bg-card);
assets/css/admin.css:29:  background: var(--primary);
assets/css/admin.css:31:  border-color: var(--primary);
assets/css/admin.css:42:  background: var(--bg-card);
assets/css/admin.css:52:  font-family: var(--font-mono);
assets/css/admin.css:58:.stat-card.highlight .stat-card-value { color: var(--primary); }
assets/css/admin.css:64:  color: var(--primary);
assets/css/admin.css:79:  background: var(--bg-card);
assets/css/admin.css:101:.btn-confirm { background: var(--primary); color: var(--ink); }
assets/css/admin.css:103:.btn-complete { background: var(--primary); color: var(--ink); }
assets/css/admin.css:126:  background: var(--bg);
assets/css/admin.css:132:  color: var(--primary);
assets/css/admin.css:150:  background: var(--bg-card);
assets/css/admin.css:171:  background: var(--bg);
assets/css/admin.css:188:  background: var(--bg);
assets/css/admin.css:191:  font-family: var(--font-display);
assets/css/admin.css:203:.modal-cancel-btn { background: var(--bg); color: var(--text-muted); border: 1px solid var(--border); }
assets/css/admin.css:212:  background: var(--primary);
assets/css/admin.css:220:  background: var(--bg-card);
assets/css/admin.css:234:  background: linear-gradient(135deg, #F5F1EA, #EDE6D9);
assets/css/admin.css:258:  background: var(--bg);
assets/css/admin.css:286:  background: var(--bg);
assets/css/admin.css:289:  font-family: var(--font-display);
assets/css/admin.css:322:  font-family: var(--font-heading);
assets/css/admin.css:343:  border-color: var(--primary);

## Dynamic rendering/XSS surfaces
./account.html:581:  playerIdList.innerHTML = '';
./account.html:589:  row.innerHTML = '<select aria-label="اللعبة" data-player-game>' +
./account.html:630:  actions.innerHTML = '';
./account.html:632:    status.innerHTML = '<span style="color:#15803d;font-weight:800"><i class="fas fa-circle-check"></i> التحقق بخطوتين مفعّل عبر Authenticator.</span>';
./account.html:634:    button.className = 'auth-btn danger'; button.type = 'button'; button.innerHTML = '<i class="fas fa-shield-halved" style="margin-left:6px"></i> إيقاف التحقق بخطوتين';
./account.html:639:    button.className = 'auth-btn secondary'; button.type = 'button'; button.innerHTML = '<i class="fas fa-mobile-screen-button" style="margin-left:6px"></i> إعداد Authenticator';
./account.html:707:  actions.innerHTML = '';
./account.html:711:    button.innerHTML = '<i class="fas fa-link-slash" style="margin-left:6px"></i> فك ربط جوجل';
./account.html:715:    button.innerHTML = '<i class="fab fa-google" style="margin-left:6px"></i> ربط حساب جوجل';
./account.html:731:    document.getElementById('googleStatusText').insertAdjacentHTML('afterend', '<div class="google-debug">تحقق من تفعيل Google ومن إضافة رابط العودة في Supabase Auth.</div>');
./admin-audit-log.html:62:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:30px 0;">لا توجد نشاطات مسجلة بعد</div>`;
./admin-audit-log.html:66:  container.innerHTML = logs.map(l => {
./admin-categories.html:187:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:30px 0;">لا توجد تصنيفات بعد</div>`;
./admin-categories.html:191:  container.innerHTML = categories.map(c => {
./admin-coupons.html:120:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:30px 0;">لا توجد كوبونات بعد</div>`;
./admin-coupons.html:124:  container.innerHTML = coupons.map(c => `
./admin-customers.html:92:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:30px 0;">لا يوجد نتائج</div>`;
./admin-customers.html:96:  container.innerHTML = filtered.map(c => {
./admin-giftcards.html:98:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:30px 0;">لا توجد كروت بعد</div>`;
./admin-giftcards.html:102:  container.innerHTML = data.map(c => `
./admin-orders.html:265:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:30px 0;">لا توجد طلبات مطابقة</div>`;
./admin-orders.html:269:  container.innerHTML = filtered.map(o => renderOrderCard(o)).join('');
./admin-orders.html:430:  if (button) { button.disabled = true; button.dataset.originalLabel = button.innerHTML; button.innerHTML = '<i class="fas fa-spinner fa-spin"></i> جارٍ التنفيذ'; }
./admin-orders.html:434:    if (button) { button.disabled = false; button.innerHTML = button.dataset.originalLabel || 'تنفيذ'; }
./admin-payment-codes.html:174:    list.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:30px 0;">لا توجد رموز مطابقة</div>`;
./admin-payment-codes.html:178:  list.innerHTML = filtered.map(c => {
./admin-payment-methods.html:165:    document.getElementById('methodsList').innerHTML =
./admin-payment-methods.html:176:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:30px 0;">لا توجد وسائل دفع بعد</div>`;
./admin-payment-methods.html:181:  container.innerHTML = methods.map(m => `
./admin-products.html:229:  row.innerHTML = `
./admin-products.html:263:  categoryInput.innerHTML = categories.map(c => `<option value="${c.id}">${c.name}</option>`).join('');
./admin-products.html:276:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:30px 0;">لا توجد منتجات بعد</div>`;
./admin-products.html:280:  container.innerHTML = products.map(p => `
./admin-products.html:318:  row.innerHTML = `
./admin-products.html:331:  fieldsBuilder.innerHTML = '';
./admin-products.html:332:  optionsBuilder.innerHTML = ''; // تنظيف الخيارات أولاً
./admin-referral-milestones.html:118:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:30px 0;">لا توجد مستويات بعد</div>`;
./admin-referral-milestones.html:122:  container.innerHTML = milestones.map(m => `
./admin-topups.html:237:    document.getElementById('topupsList').innerHTML =
./admin-topups.html:253:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:30px 0;">لا توجد طلبات في هذا التصنيف</div>`;
./admin-topups.html:257:  container.innerHTML = filtered.map(t => renderTopupCard(t)).join('');
./admin.html:199:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px;">لا توجد طلبات بعد</div>`;
./admin.html:203:  container.innerHTML = orders.map(o => `
./admin.html:224:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px;">لا يوجد عملاء بعد</div>`;
./admin.html:228:  container.innerHTML = customers.map(c => `
./assets/js/auth-ui.js:40:        btn.innerHTML = show ? ICONS.eyeOff : ICONS.eye;
./assets/js/auth-ui.js:44:      btn.innerHTML = ICONS.eye;
./assets/js/auth-ui.js:83:          li.querySelector('[data-rule-icon]').innerHTML = ok ? ICONS.checkSm : ICONS.dotSm;
./assets/js/auth-ui.js:206:      button.dataset.idleHtml = button.dataset.idleHtml || button.innerHTML;
./assets/js/auth-ui.js:209:      button.innerHTML = '<span class="spin"></span><span>' + (loadingText || 'جارِ التنفيذ...') + '</span>';
./assets/js/auth-ui.js:213:      if (button.dataset.idleHtml) button.innerHTML = button.dataset.idleHtml;
./assets/js/country-codes.js:98:      button.innerHTML =
./assets/js/country-codes.js:117:        list.innerHTML = '<div class="country-empty">لا توجد نتائج مطابقة</div>';
./assets/js/country-codes.js:121:      list.innerHTML = items.map(function (c) {
./assets/js/admin-nav.js:38:        categoriesLink.innerHTML = '<i class="fas fa-layer-group" aria-hidden="true"></i> إدارة الأقسام';
./assets/js/admin-nav.js:43:        productsLink.innerHTML = '<i class="fas fa-boxes-stacked" aria-hidden="true"></i> إدارة المنتجات والباقات';
./cart.html:350:  listContainer.innerHTML = cart.map((item, index) => {
./category.html:181:  content.innerHTML = `
./category.html:306:      subGrid.innerHTML = '';
./category.html:314:          optBtn.innerHTML = `
./category.html:415:    addToCartBtn.innerHTML = '<i class="fas fa-check-circle"></i> تم الإضافة بنجاح';
./category.html:417:      addToCartBtn.innerHTML = '<i class="fas fa-cart-plus"></i> إضافة إلى سلة المشتريات';
./category.html:443:    buyNowBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> جارِ التوجيه للدفع...';
./checkout.html:442:    document.getElementById('loadingBox').innerHTML = `
./checkout.html:500:  listContainer.innerHTML = cart.map(item => {
./checkout.html:547:    walletText.innerHTML = `رصيدك الحالي: ${safeFormatSDG(walletBalance)} ج.س (يكفي للدفع)`;
./checkout.html:550:    walletText.innerHTML = `رصيدك غير كافٍ (${safeFormatSDG(walletBalance)} ج.س) — <a href="wallet.html" class="recharge-highlight-link" onclick="event.stopPropagation();">اشحن محفظتك الآن</a>`;
./checkout.html:554:  methodPickerGrid.innerHTML = paymentMethods.map(m => `<div class="method-pill" data-method-id="${escapeAttribute(m.id || '')}">${escapeHtml(m.name)}</div>`).join('');
./checkout.html:563:      box.innerHTML = `
./checkout.html:743:    supportBox.innerHTML = `<a href="https://wa.me/${ADMIN_WHATSAPP}?text=${encodeURIComponent(msg)}" target="_blank" rel="noopener"
./checkout.html:790:      if (_b2) _b2.innerHTML = `<i class="fas fa-paperclip"></i> اضغط لرفع صورة ثانية (الإشعار الأبيض أو بقية الشاشة)`;
./checkout.html:792:    fileBox.innerHTML = `<i class="fas fa-paperclip"></i> اضغط هنا لرفع صورة إيصال التحويل`;
./checkout.html:800:    rep.innerHTML = '';
./checkout.html:902:    fileBox2.innerHTML = `<i class="fas fa-check-circle" style="color:var(--primary);"></i> صورة إضافية: ${escapeHtml(extraFile.name)}`;
./checkout.html:925:    reportEl.innerHTML = '';
./checkout.html:928:    fileBox.innerHTML = `<i class="fas fa-spinner fa-spin"></i> جارِ فحص الصورة...`;
./checkout.html:997:      reportEl.innerHTML =
./checkout.html:1015:    fileBox.innerHTML = `<i class="fas fa-check-circle" style="color:var(--primary);"></i> تم اختيار: ${escapeHtml(file.name)}`;
./checkout.html:1061:      reportEl.innerHTML =
./checkout.html:1074:    reportEl.innerHTML =
./checkout.html:1124:    shortfallBox.innerHTML = '';
./checkout.html:1141:    fileBox.innerHTML = `<i class="fas fa-triangle-exclamation" style="color:var(--danger);"></i> المبلغ في الإشعار ناقص`;
./checkout.html:1156:    shortfallBox.innerHTML =
./checkout.html:1274:    fileBox.innerHTML = `<i class="fas fa-check-circle" style="color:var(--primary);"></i> تم قبول الإشعارين (دفع مجزّأ)`;
./forgot-password.html:139:    document.getElementById('codeSub').innerHTML =
./my-orders.html:168:    container.innerHTML = `
./my-orders.html:178:  container.innerHTML = orders.map(o => renderOrderCard(o)).join('');
./notifications.html:46:    container.innerHTML = `<div style="text-align:center; padding:40px 20px; color:var(--text-muted);"><h3 style="color:var(--ink); margin-bottom:8px;">لا توجد إشعارات بعد</h3></div>`;
./notifications.html:50:  container.innerHTML = notifications.map(n => {
./product.html:274:  $('addToCartBtn').innerHTML = '<i class="fas fa-check-circle" aria-hidden="true"></i> تمت الإضافة';
./product.html:275:  setTimeout(() => { $('addToCartBtn').innerHTML = '<i class="fas fa-cart-plus" aria-hidden="true"></i> إضافة إلى السلة'; error.textContent = ''; error.style.color = ''; }, 2200);
./product.html:289:  $('content').innerHTML = `
./receipt.html:351:  document.getElementById('receiptContent').innerHTML = `
./referrals.html:141:    btn.innerHTML = '<i class="fas fa-check" style="margin-left:6px;"></i> تم النسخ';
./referrals.html:142:    setTimeout(() => { btn.innerHTML = `<i class="fas fa-copy" style="margin-left:6px;"></i> ${idleLabel}`; }, 1800);
./referrals.html:162:    box.innerHTML = `<div style="color:var(--text-muted); font-size:13px;">لا توجد مستويات مكافآت حالياً</div>`;
./referrals.html:166:  box.innerHTML = milestones.map(m => {
./referrals.html:187:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:20px 0;">لسه ما حدش سجل برابطك</div>`;
./referrals.html:191:  container.innerHTML = list.map(r => {
./referrals.html:217:    container.innerHTML = `<div style="color:var(--text-muted); font-size:13px; text-align:center; padding:20px 0;">لسه مفيش أرباح إحالة</div>`;
./referrals.html:221:  container.innerHTML = data.map(n => {
./reset-password.html:96:  document.getElementById('subText').innerHTML =
./search.html:290:    productsGrid.innerHTML = '';
./search.html:291:    categoriesGrid.innerHTML = '';
./search.html:297:  categoriesGrid.innerHTML = '';
./search.html:326:      productsGrid.innerHTML = '';
./search.html:335:      categoriesGrid.innerHTML = categoryResults.map(cat => {
./search.html:360:      productsGrid.innerHTML = productResults.map(p => `
./search.html:377:      productsGrid.innerHTML = categoryResults.length > 0 ? `<div style="color:var(--text-muted); font-size:13px; padding:10px 20px;">اضغط على اللعبة في الأعلى لعرض كافة باقاتها المتوفرة.</div>` : '';
./verify.html:69:document.getElementById('subText').innerHTML =
./wallet.html:141:    if (!method) { box.innerHTML = ''; return; }
./wallet.html:142:    box.innerHTML = `
./wallet.html:180:      document.getElementById('receiptFileLabel').innerHTML = '<i class="fas fa-cloud-arrow-up" style="margin-left:6px;"></i> اضغط لرفع صورة الإيصال (JPG / PNG / WEBP)';
./wallet.html:210:  if (report) { report.hidden = true; report.style.display = 'none'; report.className = 'receipt-scan-report'; report.innerHTML = ''; }
./wallet.html:248:  report.innerHTML = `<strong><i class="fas ${technicalReview ? 'fa-clock' : receiptAnalysisAllowed(analysis) ? 'fa-circle-check' : 'fa-circle-xmark'}"></i> ${escapeHtml(analysis.message || 'تم فحص الإيصال')}</strong><div style="margin-top:7px;">${rows.map(escapeHtml).join('<br>')}</div>`;
./wallet.html:373:    document.getElementById('balanceAmount').innerHTML = `${formatSDG(data ? data.balance : 0)} <small>ج.س</small>`;
./wallet.html:394:      container.innerHTML = `<div style="text-align:center; padding:20px; color:var(--text-muted);">لا توجد حركات على رصيدك بعد</div>`;
./wallet.html:398:    container.innerHTML = data.map(tx => {
./wallet.html:422:    container.innerHTML = `<div style="text-align:center; padding:20px; color:var(--text-muted);">تعذّر تحميل حركات الرصيد.</div>`;
./wallet.html:457:      container.innerHTML = `<div style="text-align:center; padding:20px; color:var(--text-muted);">لا يوجد طلبات شحن بعد</div>`;
./wallet.html:461:    container.innerHTML = topups.map(t => {
./wallet.html:481:    document.getElementById('topupsList').innerHTML = `<div style="text-align:center; padding:20px; color:var(--text-muted);">تعذّر تحميل سجل الشحن.</div>`;
./wallet.html:657:  document.getElementById('methodDetails').innerHTML = '';
./wallet.html:659:  document.getElementById('receiptFileLabel').innerHTML = '<i class="fas fa-cloud-arrow-up" style="margin-left:6px;"></i> اضغط لرفع صورة الإيصال (JPG / PNG / WEBP)';
./wallet.html:665:  document.getElementById('receiptFileLabel').innerHTML = '<i class="fas fa-cloud-arrow-up" style="margin-left:6px;"></i> اضغط لرفع صورة الإيصال (JPG / PNG / WEBP)';
./admin-staff.html:79:  if (!staff.length) { container.innerHTML = '<div style="color:var(--text-muted);text-align:center;padding:30px;">لا يوجد مشرفون بعد.</div>'; return; }
./admin-staff.html:80:  container.innerHTML = staff.map((person) => {
./admin-staff.html:98:  button.disabled=false; button.innerHTML='<i class="fas fa-save"></i> حفظ الصلاحيات';
./admin-staff.html:104:  container.innerHTML = '<div style="color:var(--text-muted);font-size:13px;text-align:center;padding:30px 0;"><i class="fas fa-spinner fa-spin" aria-hidden="true"></i> جارِ التحميل...</div>';
./admin-staff.html:114:    container.innerHTML = `<div class="admin-error-state"><i class="fas fa-triangle-exclamation" aria-hidden="true"></i><strong>${escapeHtml(getStaffErrorMessage(error))}</strong><button type="button" class="admin-btn btn-confirm" id="retryStaffBtn"><i class="fas fa-rotate-right"></i> إعادة المحاولة</button></div>`;
./checkout-v2.html:442:    document.getElementById('loadingBox').innerHTML = `
./checkout-v2.html:500:  listContainer.innerHTML = cart.map(item => {
./checkout-v2.html:547:    walletText.innerHTML = `رصيدك الحالي: ${safeFormatSDG(walletBalance)} ج.س (يكفي للدفع)`;
./checkout-v2.html:550:    walletText.innerHTML = `رصيدك غير كافٍ (${safeFormatSDG(walletBalance)} ج.س) — <a href="wallet.html" class="recharge-highlight-link" onclick="event.stopPropagation();">اشحن محفظتك الآن</a>`;
./checkout-v2.html:554:  methodPickerGrid.innerHTML = paymentMethods.map(m => `<div class="method-pill" data-method-id="${escapeAttribute(m.id || '')}">${escapeHtml(m.name)}</div>`).join('');
./checkout-v2.html:563:      box.innerHTML = `
./checkout-v2.html:743:    supportBox.innerHTML = `<a href="https://wa.me/${ADMIN_WHATSAPP}?text=${encodeURIComponent(msg)}" target="_blank" rel="noopener"
./checkout-v2.html:790:      if (_b2) _b2.innerHTML = `<i class="fas fa-paperclip"></i> اضغط لرفع صورة ثانية (الإشعار الأبيض أو بقية الشاشة)`;
./checkout-v2.html:792:    fileBox.innerHTML = `<i class="fas fa-paperclip"></i> اضغط هنا لرفع صورة إيصال التحويل`;
./checkout-v2.html:800:    rep.innerHTML = '';
./checkout-v2.html:902:    fileBox2.innerHTML = `<i class="fas fa-check-circle" style="color:var(--primary);"></i> صورة إضافية: ${escapeHtml(extraFile.name)}`;
./checkout-v2.html:925:    reportEl.innerHTML = '';
./checkout-v2.html:928:    fileBox.innerHTML = `<i class="fas fa-spinner fa-spin"></i> جارِ فحص الصورة...`;
./checkout-v2.html:997:      reportEl.innerHTML =
./checkout-v2.html:1015:    fileBox.innerHTML = `<i class="fas fa-check-circle" style="color:var(--primary);"></i> تم اختيار: ${escapeHtml(file.name)}`;
./checkout-v2.html:1061:      reportEl.innerHTML =
./checkout-v2.html:1074:    reportEl.innerHTML =
./checkout-v2.html:1124:    shortfallBox.innerHTML = '';
./checkout-v2.html:1141:    fileBox.innerHTML = `<i class="fas fa-triangle-exclamation" style="color:var(--danger);"></i> المبلغ في الإشعار ناقص`;
./checkout-v2.html:1156:    shortfallBox.innerHTML =
./checkout-v2.html:1274:    fileBox.innerHTML = `<i class="fas fa-check-circle" style="color:var(--primary);"></i> تم قبول الإشعارين (دفع مجزّأ)`;

## Client secrets/auth
./assets/js/receipt-pipeline.js:136:          ...(root.SUPABASE_ANON_KEY ? { apikey: root.SUPABASE_ANON_KEY } : {})
./assets/js/supabase-client.js:89:const SUPABASE_ANON_KEY = runtimeSupabaseConfig.anonKey || "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJnbGJmaXpxb2xyZW53ZnNuZHl2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODMxNDY4NzMsImV4cCI6MjA5ODcyMjg3M30.bJywsPvgXPdsNOZlVTIwYHz3Z2zcobwinGuUXAb5ev4";
./assets/js/supabase-client.js:92:window.SUPABASE_ANON_KEY = SUPABASE_ANON_KEY;
./assets/js/supabase-client.js:97:  supabaseClient = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY, {
./supabase/functions/process-receipt/index.ts:232:  const serviceKey = env("SUPABASE_SERVICE_ROLE_KEY");
./supabase/functions/process-receipt/index.ts:317:  const apiKey = env("GEMINI_API_KEY");

## Supabase calls
312

## Images/forms/interactive rough counts
img=22
forms=8
buttons=159

## Runtime audit — Agent Browser 393px

تم فتح `https://raizey-store-qw.vercel.app/index.html` بعرض 393×852. الصفحة الرئيسية فتحت وظهرت عناصر التنقل والـ hero والمساعد والواتساب. Axe 4.12.1 سجّل مخالفتين: ثلاث عقد `color-contrast` في `span[dir="ltr"]` وfooter paragraph، ومخالفة `meta-viewport` بسبب تعطيل التكبير/التصغير، مع مراجعة يدوية مطلوبة لتباين `.slide-1 > .slide-eyebrow`. لم تُجرَ أي تعديلات بعد تسجيل هذه النتائج.

## Runtime audit — local patched home

بعد إزالة تعطيل zoom وتحديد transition properties وتغميق النص الثانوي ولون شعار STORE، أعاد Axe 4.12.1 النتيجة `violations: 0`, `passes: 12`. بقيت ملاحظة manual واحدة على `.slide-1 > .slide-eyebrow` بسبب conservative contrast review، دون violation آلي.

## Runtime audit — auth/payment public routes

Agent Browser على 393px: `login.html` اجتازت violations الآلية في Axe (`violations: 0`, `passes: 20`) مع ملاحظات manual contrast على عناصر الهوية والحقول. فتح `wallet.html` بدون جلسة أعاد التوجيه إلى `login.html`، وهو سلوك authorization متوقع؛ لم تُرسل بيانات ولم تُنفذ عملية مالية.
