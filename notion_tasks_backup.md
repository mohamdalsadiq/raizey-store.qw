# نسخة احتياطية لمهام Raizey Store من Notion

> المصدر: قاعدة Notion «Raizey Store — خطة التطوير القادمة».
>
> ملاحظة: تم حفظ هذه النسخة محليًا من المحتوى الذي تم جلبه بنجاح. تفاصيل المهام 2–5 الكاملة تحتاج إعادة اتصال Notion لإكمال النسخ دون تخمين.

## حالة المهام وترتيبها

| الترتيب | المهمة | التعقيد | الحالة عند آخر قراءة | يحتاج SQL |
|---:|---|---|---|---|
| 1 | المهمة 1 — تفعيل باقي مزايا المصادقة الموجودة في إعدادات Supabase | كبير | مكتمل بعد تأكيد المستخدم | لا |
| 2 | المهمة 2 — إعادة تصميم صفحة إعدادات الحساب (account.html) باحترافية كاملة | كبير جداً | لم يبدأ عند آخر قراءة | نعم |
| 3 | المهمة 3 — تفعيل الفحص الذكي في صفحة شحن المحفظة + إعادة تصميمها | متوسط | لم يبدأ عند آخر قراءة | لا |
| 4 | المهمة 4 — تحسين صفحة الإحالات + كود إحالة قصير + إصلاح خانة رمز الدولة | كبير | لم يبدأ عند آخر قراءة | نعم |
| 5 | المهمة 5 — تقسيم الصفحة الرئيسية لأقسام + تحكم إداري + تحسين الخطوط والأيقونات عموماً | كبير جداً | لم يبدأ عند آخر قراءة | نعم |

---

# المهمة 1 — تفعيل باقي مزايا المصادقة الموجودة في إعدادات Supabase

## الخلفية

في Supabase → Authentication → Emails توجد 6 قوالب بريد جاهزة، لكن المتجر كان يستخدم قالبين فقط: تأكيد التسجيل وإعادة تعيين كلمة السر. الباقي معرّف في Supabase ولكن لا توجد واجهة في الموقع تستخدمه.

## المطلوب

### أ) الدخول بكود لمرة واحدة Magic Link / OTP

- إضافة زر اختياري في `login.html` باسم «الدخول بكود عبر البريد» بجانب الدخول بكلمة السر.
- يكتب المستخدم بريده فقط، ثم يُستدعى `supabaseClient.auth.signInWithOtp({ email })`.
- يصل المستخدم كود من 6 أرقام، ثم يُدخل الكود ليُسجّل دخوله عبر `verifyOtp({ email, token, type: 'email' })`.
- يجب تعديل قالب بريد Magic Link في Supabase يدويًا باستبدال `{{ .ConfirmationURL }}` بـ `{{ .Token }}`، كما في قالب استعادة كلمة السر.

### ب) تغيير البريد الإلكتروني بتأكيد

- جعل حقل البريد في `account.html` قابلًا للتعديل.
- عند الحفظ يُستدعى `supabaseClient.auth.updateUser({ email: newEmail })`.
- Supabase يرسل تلقائيًا رسالة تأكيد إلى العنوان القديم والجديد عند تفعيل Secure Email Change.
- تظهر رسالة واضحة: «تحقق من بريدك الجديد (والقديم) لتأكيد التغيير».
- تبقى شارة «غير مؤكَّد» على الحقل إلى أن يكتمل التأكيد.
- يجب التأكد يدويًا من أن قالب Change Email Address مفعّل.

### ج) إعادة المصادقة قبل العمليات الحساسة

قبل تغيير كلمة السر من داخل الحساب، تغيير البريد، أو فك ربط جوجل، يجب طلب تأكيد الهوية بكود يصل إلى البريد أو بكلمة السر الحالية.

تم تنفيذ هذا التدفق في `account.html` باستخدام نافذة تأكيد وكود OTP، مع استخدام `reauthenticate()` و`updateUser({ password, nonce })` وفقًا لتوثيق Supabase الحالي.

## اختبار القبول

- الدخول بكود عبر البريد يعمل من البداية حتى تسجيل الدخول الفعلي.
- تغيير البريد يرسل تأكيدًا فعليًا ولا يتغير البريد إلا بعد التأكيد.
- تغيير كلمة السر أو البريد أو فك ربط جوجل يطلب تأكيد الهوية أولًا.

## التسليم العام

ملف ZIP يحتوي فقط الملفات المعدلة بمساراتها الكاملة، وأي SQL مطلوب يوضع في ملف باسم `supabase-SQL-[اسم المهمة].sql` داخل ZIP.

## الملفات المعدلة للمهمة 1

- `account.html`
- `login.html`
- `assets/css/auth.css`

## حالة التنفيذ

أكد المستخدم أن الفحص اليدوي نجح وأن كل شيء يعمل بشكل ممتاز. تم تسليم `/home/ubuntu/raizey-store-task1.zip`.


## تحديث الوصول — 2026-08-17

تم فتح الرابط مباشرة في المتصفح بنجاح. ظهرت قاعدة «Raizey Store — خطة التطوير القادمة» وظهرت المهام الخمس وحالاتها. عند هذه القراءة ظهرت المهمة 1 بحالة «قيد التنفيذ» في الواجهة، بينما المهام 2–5 بحالة «لم يبدأ». سأستخرج تفاصيل كل صفحة مهمة وأحدّث هذه النسخة الاحتياطية.

رابط القاعدة: https://app.notion.com/p/712da759704d43b28c9a3f5d400f686a?v=63230696521d49e9bcc397ace3577882&source=copy_link


## ملاحظة استخراج الصفحات

الجدول ظاهر في المتصفح ويمكن قراءة أسماء المهام وحالاتها، لكن النقر المباشر على صف المهمة الثانية لم يفتح صفحة التفاصيل. سأستخرج روابط الصفوف من HTML المحفوظ محليًا للوصول إلى صفحات المهام دون تخمين.


## فحص DOM

تم الوصول إلى الجدول مباشرة عبر الرابط، لكن صفوف قاعدة البيانات تظهر كعناصر داخلية دون `href` ظاهر في DOM؛ النقر المباشر لم يفتح صفحة المهمة. تم تأكيد وجود عنوان المهمة الثانية نصيًا داخل عنصر الجدول، وسأستخدم أدوات المتصفح/مصدر الصفحة لاستخراج معرفات السجلات.


## عودة إلى القاعدة

تمت العودة إلى قاعدة Notion بنجاح، وظهر وصفها: «قائمة المهام المفصّلة للمرحلة القادمة من تطوير المتجر، مرتبة حسب الأولوية. قواعد التسليم العامة موجودة في كل مهمة.» ما زالت المهمة 1 تظهر «قيد التنفيذ» في الواجهة، والمهمة 2 تظهر «لم يبدأ».


## معرف السجل المستخرج

تم استخراج معرف سجل المهمة الثانية من DOM: `3be180c6-b36c-81c4-806b-dfedf2d9ea44`. لم يُفتح الرابط الخام مباشرة، لذلك تم تفعيل حدث النقر على سجل قاعدة البيانات نفسه، وننتظر انتقال الواجهة إلى تفاصيل الصفحة.


## محاولة فتح الصف

لم تنتقل الواجهة بعد النقر البرمجي أو النقر المزدوج، مع أن سجل المهمة الثانية يحمل `data-block-id` صحيحًا. سأستخرج بيانات الصفحة من حالة Notion أو أستخدم أدوات المتصفح لفتح السجل عبر التفاعل المرئي.


## نتيجة التفاعل الأخير

تمت محاولة فتح المهمة الثانية عبر فهرس العنصر `29` وبالنقر الإحداثي مرتين، لكن Notion أبقى الجدول مفتوحًا ولم يعرض صفحة التفاصيل. لم يتم تغيير أي بيانات.


# المهمة 2 — إعادة تصميم صفحة إعدادات الحساب (account.html) باحترافية كاملة

## بيانات المهمة

- الترتيب: 2
- التعقيد: كبير جداً
- الحالة عند القراءة: لم يبدأ
- يحتاج SQL: نعم

## الهدف العام

إعادة تصميم `account.html` بالكامل بمظهر احترافي، وأيقونات فاخرة متسقة مع هوية المتجر من دون إيموجي، وتقسيم واضح إلى أقسام منطقية. تعتمد المهمة على المهمة 1، خصوصًا أجزاء المصادقة.

## الأقسام المطلوبة

### 1) الأمان والخصوصية

- تغيير كلمة السر: حقل مع مؤشر قوة باستخدام نفس مكوّن `register.html` لضمان التناسق، ومحمي بإعادة المصادقة من المهمة 1.
- التحقق بخطوتين 2FA عبر تطبيق Authenticator بنظام TOTP باستخدام `auth.mfa.enroll` في Supabase. خيار الواتساب/SMS مؤجل لأنه يحتاج مزودًا خارجيًا مثل Twilio وتكلفة إضافية، ويظهر في الصفحة كـ «قريباً» فقط.
- الأجهزة النشطة: لأن Supabase لا تعطي من طرف العميل قائمة كاملة بكل الجلسات، يجب تقديم زر واحد «تسجيل الخروج من كل الأجهزة» باستخدام `signOut({ scope: 'global' })` بدل جدول جلسات.
- ربط الحسابات: ربط وفك ربط جوجل باستخدام `linkIdentity` و`unlinkIdentity`، مع حماية العمليات بإعادة المصادقة.

### 2) البيانات الشخصية

- الاسم الكامل ورقم الواتساب موجودان، ويستمران مع تصميم أفضل.
- البريد مع شارة «موثّق ✓» أو زر «إرسال تأكيد» إذا كان غير موثّق، مرتبطًا بالمهمة 1-ب.
- معرّفات لاعب افتراضية: حقل أو حقول لحفظ Player ID المفضّل، مثل PUBG ID، لملئه تلقائيًا عند الشراء. يتطلب عمودًا جديدًا في جدول `profiles` من نوع jsonb لتخزين أكثر من لعبة، مع تعديل بسيط في `category.html` للقراءة والتعبئة تلقائيًا إن وجد، وهو تحسين اختياري إذا سمح الوقت.

### 3) المحفظة والتفضيلات

- عملة العرض المفضلة اختيار بصري فقط، طالما التسعير الفعلي ثابت بالجنيه السوداني.
- صندوق معلومات يوضح سياسة شحن المحفظة وحماية الرصيد.

### 4) نصوص السياسات — تُستخدم حرفيًا

| العنصر | النص |
|---|---|
| حماية البيانات | بياناتك الشخصية ومعاملاتك المالية مشفرة بالكامل ولا يتم مشاركتها مع أي طرف ثالث. |
| سياسة الاسترجاع | البطاقات والخدمات التي تم تسليمها بنجاح غير قابلة للإرجاع إلا في حال وجود خلل في الكود. |
| التأكيد قبل الشراء | تأكد دائماً من صحة الآيدي (Player ID) المرفق قبل تأكيد الطلب، المتجر غير مسؤول عن الإدخال الخاطئ. |
| حسابات التواصل الرسمية | لتجنب الاحتيال، تحقق دائماً من أنك تتواصل مع الدعم الفني عبر رقم الواتساب الرسمي المعتمد في المتجر. |

## ملاحظات التصميم

استخدام أيقونات Font Awesome الموجودة أصلًا في المشروع بدل الإيموجي، مع نفس نظام الألوان البرتقالي والأبيض/الكريمي، وتحسين التباعد والترتيب البصري.

## اختبار القبول

- كل قسم من الأقسام الأربعة موجود وواضح.
- تغيير كلمة السر والبريد يعمل فعليًا عبر تدفق إعادة المصادقة.
- ربط وفك ربط جوجل يعملان.
- النصوص القانونية الأربعة ظاهرة حرفيًا كما وردت أعلاه.


## معرفات سجلات المهام

| المهمة | معرف السجل |
|---|---|
| المهمة 1 | `3be180c6-b36c-81aa-9e3c-f3a1f8ad2e48` |
| المهمة 2 | `3be180c6-b36c-81c4-806b-dfedf2d9ea44` |
| المهمة 3 | `3be180c6-b36c-819b-93f9-e5f06adba7fa` |
| المهمة 4 | `3be180c6-b36c-81cb-b91d-dcc5b8a4875c` |
| المهمة 5 | `3be180c6-b36c-8133-b3ac-e69a51ba20d3` |

صيغة الوصول العام التي نجحت للمهمة 2 هي `https://www.notion.so/<المعرف بدون الشرطات>`، وقد أعاد الرابط إلى صفحة `app.notion.com/p/<المعرف>` مع محتوى المهمة.


# المهمة 3 — تفعيل الفحص الذكي في صفحة شحن المحفظة + إعادة تصميمها

## بيانات المهمة

- الترتيب: 3
- التعقيد: متوسط
- الحالة عند القراءة: لم يبدأ
- يحتاج SQL: لا

## المشكلة الحالية

`wallet.html` لشحن المحفظة عبر تحويل بنكي لا يستخدم محرك الفحص الذكي نفسه المفعّل في `checkout.html`، أي أن إيصالات شحن المحفظة تُفحص يدويًا بالكامل حاليًا.

## المطلوب

ربط نفس مسار الفحص المستخدم في `checkout.html` داخل `wallet.html` بالمنطق نفسه: استدعاء `/api/verify-receipt` أولًا، ثم fallback إلى Tesseract في المتصفح عند الفشل، ثم مراجعة يدوية fail-closed. يجب تجنب تكرار الكود إن أمكن واستدعاء الدوال المشتركة من `receipt-judge-core.js` و`receipt-intel.js`.

يجب التأكد من استدعاء `claim_payment_receipt` الموجودة في قاعدة البيانات بالطريقة نفسها من مسار شحن المحفظة، حتى يعمل منع تكرار رقم العملية وربط `ocr_status` بالقوة نفسها الموجودة في دفع المنتجات.

## إعادة التصميم

تصميم أنيق واحترافي دون إيموجي أو أيقونات كرتونية، باستخدام Font Awesome بأسلوب متسق مع بقية المتجر، مع نفس هوية الألوان وتحسين ترتيب خطوات الشحن بصريًا.

## اختبار القبول

- رفع إيصال شحن محفظة يمر فعليًا عبر Gemini وتظهر نتيجة الفحص خلال ثوانٍ.
- عند فشل السيرفر يرجع التدفق تلقائيًا إلى تسلسل fallback نفسه المستخدم في دفع المنتجات.
- رقم عملية مستخدم سابقًا، سواء في دفع منتج أو شحن محفظة، يُرفض في الحالتين بمنطق موحّد.


# المهمة 4 — تحسين صفحة الإحالات + كود إحالة قصير + إصلاح خانة رمز الدولة

## بيانات المهمة

- الترتيب: 4
- التعقيد: كبير
- الحالة عند القراءة: لم يبدأ
- يحتاج SQL: نعم

## أ) إعادة تصميم `referrals.html`

تحسينات بصرية فقط، تشمل الخطوط والأزرار والتباعد، مع نفس نظام الألوان الحالي، ودون تغيير المنطق أو هيكلة المعلومات.

## ب) نظام كود إحالة قصير من 5–6 خانات

قبل التنفيذ يجب فحص جداول ودوال الإحالة الموجودة فعليًا في قاعدة البيانات. الصفحة الحالية تشير إلى نظام إحالة يعمل عبر رابط `?ref=CODE`، والمطلوب إضافة طريقة ثانية للاستخدام: كود قصير يُكتب يدويًا بجانب الرابط، لا استبداله.

كل مستخدم يحصل على كود قصير من 5–6 حروف أو أرقام، مثل `RZY482`، يُنشأ تلقائيًا عند التسجيل ومربوط بنفس منطق الإحالة الموجود حاليًا، مع التأكد من الجدول ودالة الاحتساب.

تُضاف خانة كود إحالة اختيارية في `register.html` تحت خانة الواتساب أو في موضع بصري مناسب. إذا كتب المستخدم كودًا صحيحًا، تُحسب الإحالة تلقائيًا بالطريقة نفسها التي تعمل بها الروابط حاليًا.

التحقق من الكود يكون فوريًا أو عند الإرسال بحسب الأسهل تقنيًا مع الحفاظ على الأمان. لا يُقبل كود مستخدم للحساب نفسه، ولا كود المستخدم نفسه.

## ج) إصلاح أعلام خانة رمز الدولة في `register.html`

إزالة صور الأعلام بالكامل والاكتفاء بعرض اسم الدولة ورمز الاتصال نصيًا في القائمة المنسدلة، مثل «السودان +249»، لتفادي الأيقونات المكسورة على Android Chrome وعدم الاعتماد على صور أو خطوط إيموجي.

## اختبار القبول

- كود إحالة قصير يظهر لكل مستخدم في `referrals.html`.
- كتابة كود صحيح في التسجيل تُسجّل كإحالة ناجحة بموثوقية الرابط.
- كود مستخدم من قبل أو كود خاطئ أو كود المستخدم نفسه لا يُقبل.
- خانة رمز الدولة تظهر بلا أيقونات مكسورة على أي جهاز.


# المهمة 5 — تقسيم الصفحة الرئيسية لأقسام + تحكم إداري + تحسين الخطوط والأيقونات عموماً

## بيانات المهمة

- الترتيب: 5
- التعقيد: كبير جداً
- الحالة عند القراءة: لم يبدأ
- يحتاج SQL: نعم

## أ) تقسيم `index.html` إلى أقسام وفئات

إنشاء نظام أقسام مثل الألعاب والاشتراكات وبطاقات الدفع، بحيث يظهر كل قسم بعنوانه ومنتجاته في الصفحة الرئيسية بدل عرض كل المنتجات في قائمة واحدة.

يتطلب ذلك جدولًا جديدًا `categories` يحتوي الاسم والترتيب وحالة التفعيل، وعمود `category_id` في جدول `products`، مع دالة أو استعلام لجلب المنتجات مجمعة حسب القسم.

تضاف لوحة تحكم أو قسم جديد في `admin.html` لإدارة الأقسام بالإضافة والتعديل والحذف والترتيب، وربط كل منتج بقسمه من `admin-products.html` الحالية.

## ب) جعل نص «تسليم فوري ومضمون 100%» قابلًا للتحكم

إضافة عمود `delivery_badge_text` في جدول `products`، يقبل نصًا فارغًا. في `admin-products.html` يضاف حقل لكل منتج باسم «نص شارة التسليم»؛ إذا تُرك فارغًا فلا تعرض البطاقة الشارة. في `index.html` و`category.html` تعرض القيمة المخصصة بدل النص الثابت أو تُخفى الشارة عند فراغها.

## ج) تحسين الخطوط والأيقونات على مستوى المتجر

اختيار خط عربي احترافي مناسب، بتحسين خط Tajawal الحالي أو استبداله بخط أقوى، وتطبيقه بثبات على كل الصفحات. تحسين الأيقونات لتكون فخمة ومتسقة باستخدام Font Awesome، خصوصًا في الصفحة الرئيسية و`category.html` وبقية الصفحات.

هذا تحسين فوق التصميم الحالي لا إعادة بناء من الصفر؛ تبقى الألوان والتصميم العام كما هي مع صقلها وتحسينها.

## د) تحسين `category.html` بصريًا

تحسين العرض والسلاسة والانسيابية مع الحفاظ على البنية الوظيفية: اختيار الباقة وإدخال البيانات والسعر اللحظي وغيرها يجب أن تبقى تعمل كما هي.

## اختبار القبول

- الصفحة الرئيسية تعرض المنتجات مقسمة إلى أقسام واضحة.
- الأدمن يستطيع إضافة وترتيب الأقسام وربط أي منتج بقسم من لوحة التحكم.
- نص شارة التسليم قابل للتعديل أو الإخفاء لكل منتج من الأدمن.
- الصفحات الرئيسية و`category.html` تستخدم خطًا وأيقونات محسّنة ومتسقة من دون كسر الوظائف.


## فحص المهمة الثانية — اختبار محلي

نجح `TASK2_VALIDATION_OK` ومرّ `git diff --check`. عند فتح `account.html` محليًا من دون جلسة مستخدم، أعاد التطبيق إلى `login.html` كما هو متوقع لحماية الصفحة. الاختبار البصري الكامل للأقسام داخل الحساب يحتاج جلسة مستخدم مصادق عليها؛ لم يتم تجاوز الحماية أو إرسال بيانات تجريبية.


## تنفيذ المهمة الثانية — الحالة الحالية

تمت إعادة تصميم `account.html` وإضافة الأقسام الأربعة المطلوبة: الأمان والخصوصية، البيانات الشخصية، المحفظة والتفضيلات، والسياسات. أضيف مؤشر قوة كلمة السر المتسق مع `register.html`، و2FA عبر Supabase TOTP، وزر تسجيل الخروج العالمي، وربط وفك ربط جوجل، وشارة حالة البريد وإعادة الإرسال، وحفظ Player IDs، وتفضيل العملة البصري، وصندوق معلومات المحفظة، ونصوص السياسات الأربعة حرفيًا.

أضيف ملف `supabase-SQL-المهمة-2.sql` لإنشاء عمود `player_ids` بصيغة JSONB مع قيمة افتراضية وقيد يتحقق من أن القيمة كائن JSON.

نتيجة الفحص: `TASK2_VALIDATION_OK` و`git diff --check` بلا أخطاء. الاختبار التفاعلي الكامل يحتاج جلسة مستخدم؛ الصفحة أعادت غير المسجل إلى `login.html` كما هو متوقع.
