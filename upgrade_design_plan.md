# مخطط التوسعة الشاملة

## تجربة المتجر

تُرتّب الصفحة الرئيسية الأقسام التسويقية بهذا التسلسل: الأكثر شراءً، الاشتراكات، الألعاب، ثم بقية الأقسام الفعالة. القسم التسويقي لا يكرر منطق الشراء؛ كل بطاقة قسم تقود إلى `category.html?id=...`، وهناك يختار العميل المنتج/الباقة والخيارات والحقول المطلوبة، ثم يرى السعر اللحظي قبل الإضافة للسلة أو الشراء.

سيُستخرج قسم الأكثر شراءً من المنتجات التي لها طلبات مكتملة، مع fallback إلى ترتيب المنتجات إذا لم تتوافر بيانات كافية. ولتجنب استعلامات كبيرة، يستخدم الاستعلام aggregate محدودًا في RPC أو بيانات طلبات حديثة محدودة، ولا يُنفذ تجميع كامل في كل بطاقة.

## العروض التلقائية

يستخدم النظام الحقول الموجودة فعليًا في `products`: السعر الحالي `price_usd`، السعر السابق `compare_at_price_usd`، ونهاية العرض `flash_sale_ends_at`. العرض صالح فقط إذا كان المنتج فعالًا، والسعر السابق أكبر من السعر الحالي، والتاريخ لم ينتهِ. تُحسب نسبة الخصم في المتصفح للعرض فقط، وتظل قيمة السعر النهائية محمية بالتحقق الخادمي الموجود عند الطلب. يظهر شريط إعلان متحرك فوق البنر عندما يوجد عرض فعال، ويُختار المنتج من لوحة المنتجات دون كتابة إعلان يدوي.

## لوحة الإدارة

تُوحّد كل صفحات الإدارة على شريط علوي واضح، بطاقات إحصاء مدمجة، مساحات 8px، حالات تحميل/فراغ/خطأ، وشارات حمراء للطلبات قيد المراجعة، طلبات التدقيق الإداري، وشحن المحافظ المعلقة. صفحة الطلبات ستستخدم قاموس حالات كاملًا، وستعرض تبويبات عملية مع العدادات، وتتعامل مع `needs_admin_check` باعتباره وسم تدقيق لا حالة طلب مستقلة عندما تكون قيمة status غير موجودة في المخطط.

## صلاحيات المشرفين

تُضاف صفحة `admin-staff.html` لإدارة الحسابات التي دورها admin فقط. السوبر أدمن يرى ويعدل سجلات `admin_permissions`; المشرف العادي لا يرى رابط الصفحة ولا يستطيع الوصول إليها أو تنفيذ عملياتها. الصلاحيات هي إدارة الطلبات، المحافظ، المنتجات، الإعدادات، الصيانة، المشرفين، الكوبونات، كروت الهدايا، وسجل التدقيق، مع خيار "كل الصلاحيات" يحفظ `is_super_admin` أو يملأ الصلاحيات بشكل صريح وفق السياسة الحالية.

لن تستخدم الصفحة تعديل `profiles.role` مباشرة من العميل. الترقية وإنشاء الصلاحيات يتمان عبر RPC محمية بـ `is_super_admin()`, مع منع تعديل حساب السوبر أدمن الحالي أو حذف آخر سوبر أدمن، وتسجيل كل تغيير في سجل التدقيق.

## التنبيهات

التنبيه الأحمر في التنقل والإحصاءات يعتمد على استعلامات count محدودة: `orders.status = pending_review`, وطلبات التدقيق عبر `ocr_status = needs_admin_check OR review_reason IS NOT NULL`, و`wallet_topups.status = pending`. لا نستخدم polling سريعًا؛ تُحدّث العدادات عند فتح الصفحة وبعد كل عملية، ويمكن تفعيل Realtime لاحقًا بمرشحات الجداول بعد اختبار سياسات RLS.

## الخطوط

يُعتمد `IBM Plex Sans Arabic` للنصوص الأساسية وواجهات الإدارة، و`Noto Sans Arabic` للعناوين والإعلانات الكبيرة، و`JetBrains Mono` للأرقام والأسعار والمراجع. تُحافظ الأوزان والمساحات على التسلسل الحالي، ولا يُستبدل الخط آليًا داخل النصوص الرقمية.
