# تدقيق التوسعة الشاملة — Raizey Store

## ما ثبت من الواجهة الحالية

- الصفحة الرئيسية تعرض أقسامًا مع منتجات مجمعة، لكنها تعتمد على أسماء الأقسام دون مفهوم صريح للأولوية أو قسم `الأكثر شراءً`، ولا تزال كل بطاقة تقود مباشرة إلى `category.html` من دون طبقة تصفح أقسام تسويقية واضحة.
- `category.html` هي صفحة اختيار المنتجات والباقات فعليًا، وتحتوي على الخيارات الفرعية والحقول المطلوبة والسعر اللحظي، لكنها تحتاج صقلًا بصريًا لتقليل حجم أزرار الخيارات وتحسين منطقة السعر.
- `admin-orders.html` يعرض زر `يحتاج تدقيق` لكنه لا يضيف `needs_admin_check` ضمن `statusLabels`، كما أن إجراءات التأكيد/الإكمال تستخدم تحديثًا مباشرًا من الواجهة بدل مسار موحد يتحقق من الصلاحية والحالة ويعالج الأخطاء بوضوح. لا توجد أزرار لمسارات `cancelled` أو `needs_admin_check` داخل بطاقة الطلب.
- معظم صفحات الإدارة تستخدم شرط `profile.role === 'admin'` فقط، رغم وجود جدول صلاحيات فعلي باسم `admin_permissions` ودالة `has_admin_permission` في البنية الحالية؛ هذا يجعل الواجهة غير متوافقة مع نظام المشرفين الجزئي الموجود.
- `admin-customers.html` يتيح ترقية أي عميل إلى `admin` وتغيير الدور مباشرة من المتصفح، ولا يوفر واجهة صلاحيات دقيقة أو حماية سوبر أدمن في المسار المرئي.
- لوحة الإدارة الحالية تعمل بصريًا على الهاتف، لكنها تعتمد على تنقل أفقي طويل، فراغات ثابتة، وأسلوب غير موحد بين البطاقات والنماذج. توجد أيضًا روابط مفقودة أو غير متناسقة لبعض صفحات الإدارة.

## ما ثبت من Supabase الفعلي

المشروع الفعلي هو `raizey-store` بالمعرف `rglbfizqolrenwfsndyv`.

- `public.orders` يحتوي على الحالات `pending_review`, `in_progress`, `completed`, `cancelled`, و`rejected`، إضافة إلى `ocr_status`, `review_reason`, و`review_severity`.
- `public.wallet_topups` يحتوي على `pending`, `confirmed`, و`rejected`، مع RPCs ذرية موجودة لتأكيد ورفض الشحن.
- `public.notifications` موجود ومحمى بـ RLS، ويستخدمه المتجر لإشعارات العملاء؛ لا يوجد نظام إشعار مخصص للأدمن، لذلك التنبيه الأحمر الأنسب في لوحة الإدارة هو عدادات مباشرة من الطلبات والشحنات وحالات التدقيق، مع اشتراك Realtime اختياري لاحقًا.
- `public.admin_permissions` موجود ويحتوي على: `is_super_admin`, `can_manage_orders`, `can_manage_wallets`, `can_manage_products`, `can_manage_settings`, `can_manage_maintenance`, `can_manage_admins`, `can_manage_coupons`, `can_manage_gift_cards`, و`can_view_audit_logs`.
- سياسات RLS الحالية تمنح `orders_admin_all` و`topups_admin_all` بناءً على `is_admin()` العام، بينما التفاصيل الدقيقة للصلاحيات مفروضة داخل بعض RPCs فقط. يجب عدم الاكتفاء بإخفاء الروابط؛ يجب إضافة تحقق صلاحيات في كل صفحة ومسار تغيير حساس.
- سياسة `admin_permissions` تمنح صاحب السجل أو السوبر أدمن القراءة، وتمنح السوبر أدمن فقط الكتابة على سجلات المشرفين الآخرين مع منع تعديل سجله عبر الشرط الحالي. ينبغي الحفاظ على هذه الحماية وعدم توفير تعديل مباشر للدور من صفحة العملاء.
- `products` يحتوي بالفعل على `compare_at_price_usd` و`flash_sale_ends_at`، ما يسمح ببناء عروض تلقائية دون عمود جديد: السعر الحالي هو سعر العرض، والسعر المقارن هو السعر السابق، وتاريخ النهاية يحدد صلاحية الإعلان.

## قرار الخطوط

بعد مراجعة مصادر Google Fonts، الاقتران المقترح هو `IBM Plex Sans Arabic` للنصوص والواجهة ولوحة الإدارة، و`Noto Sans Arabic` للعناوين والإعلانات الكبيرة عند الحاجة، مع `JetBrains Mono` للأرقام والمراجع والأسعار. كلا الخطين العربيين يقدمان أوزانًا متعددة مناسبة للتدرج البصري على الهاتف؛ يجب اختبارهما على النصوص الطويلة والأرقام قبل تعميمهما.

## المخاطر التي يجب ألا تتكرر

- عدم تنفيذ صلاحية المشرف في الواجهة فقط؛ يجب أن تكون الصلاحية حقيقية في RPC/RLS.
- عدم تحديث سعر العرض أو العمولة من العميل دون تحقق خادمي.
- عدم عرض نصوص أو روابط ديناميكية داخل `innerHTML` من دون escaping أو `sanitizeUrl`.
- عدم تشغيل Realtime أو polling عدواني؛ التنبيهات الأساسية تُحسب باستعلامات محدودة، ويمكن إضافة Realtime بمرشح آمن إذا كان مطلوبًا لاحقًا.

## نتيجة المعاينة المحمولة

تمت معاينة `upgrade-index-mobile.png` بعرض 393 بكسل؛ ظهر الشعار والبنر والشريط المميز ومقدمة الكتالوج دون قص أو تمرير أفقي. المنتجات لم تظهر لأن التشغيل المحلي لا يملك بيانات Supabase حية، وهو fallback متوقع.

تمت معاينة `upgrade-category-mobile.png` من نسخة مؤقتة لا تستدعي الجلسة؛ ظهر زر الرجوع والسلة وحالة التحميل بشكل متوازن. تفاصيل أزرار الباقات والسعر اللحظي تحتاج جلسة وبيانات منتجات فعلية لإتمام المعاينة.

## مصادر الخطوط الخارجية

- [Google Fonts — Noto Sans Arabic](https://fonts.google.com/noto/specimen/Noto+Sans+Arabic): خط Sans Serif عربي متعدد الأوزان والعروض، وتعرض صفحة Google Fonts أوزانًا من Thin إلى Black.
- [Google Fonts — IBM Plex Sans Arabic](https://fonts.google.com/specimen/IBM+Plex+Sans+Arabic): خط Sans Serif عربي متعدد الأوزان، مناسب للنصوص وواجهات الويب والهاتف.

الاقتران المعتمد في الكود: `IBM Plex Sans Arabic` للنصوص الأساسية، `Noto Sans Arabic` للعناوين، و`JetBrains Mono` للأرقام والأسعار.

## معاينة لوحة الإدارة المحمولة

تمت معاينة `admin-orders.html` بعرض 393 بكسل؛ ظهر العنوان، رابط المتجر، التنقل، البحث، والتبويبات في تخطيط متجاوب. التبويبات الطويلة تبقى ضمن شريط أفقي محدود بدل تمديد الصفحة، وتظهر حالة التحميل في مساحة مستقلة.

تمت معاينة `admin-staff.html` بالعرض نفسه؛ ظهرت مقدمة حماية السوبر أدمن والتنسيق العام بصورة واضحة. بيانات المشرفين والصلاحيات لا تظهر في المعاينة المحلية لأنها تحتاج جلسة Supabase حقيقية، وهذا متوقع.


## تحديث إعادة الهيكلة — معاينة الهاتف

تمت معاينة صفحة الباقات بعرض 393px باستخدام بيانات تجريبية: ظهر تسلسل القسم ثم عنوان الباقات ثم بطاقات PUBG وFree Fire، مع السعر والشارة وزر فتح الباقة، دون خروج أفقي أو قص في النصوص. ظهرت الصور الاحتياطية بأيقونة صندوق عند غياب الصورة، وهو سلوك مقصود وآمن.

تمت معاينة صفحة إدارة المشرفين بعرض 393px: ظهر العنوان والتنقل والمقدمة بشكل متناسق، مع بقاء منطقة الفريق في حالة التحميل لأن المعاينة المحلية لا تحتوي جلسة Supabase حقيقية. مسار الخطأ الجديد يعرض رسالة آمنة وزر إعادة المحاولة عند فشل الاستعلام.

تم اختيار Cairo كخط موحد في صفحات HTML، مع Tajawal كاحتياطي وJetBrains Mono للأرقام والأسعار، وتحديث tokens العامة في style.css.


## إصلاح تدفق الباقة والسلة — معاينة الهاتف

تمت معاينة product.html بعرض 393px بعد الإصلاح: ظهرت أزرار الزيادة والنقصان بجانب الكمية، وظهر الإجمالي الحالي، وزر متابعة الدفع، وزر إضافة إلى السلة. لم تعد خيارات المحفظة أو التحويل أو رفع الإيصال تظهر داخل صفحة الباقة؛ هذه الخطوات ستُنفذ في checkout.html بعد الانتقال.

تمت معاينة admin-catalog.html بعرض 393px: ظهر التسلسل الإداري الجديد للقسم ثم الباقة ثم الخيارات، مع تصفية الأقسام وحالة منع آمنة عند عدم وجود صلاحية manage_products أو جلسة أدمن.


## الخيار الأساسي والفرعي الإجباري — معاينة الهاتف

تمت معاينة منتج RedotPay بعرض 393px. ظهر الخيار الأساسي RedotPay أولًا، وبقيت الخيارات الفرعية مخفية حتى اختيار الخيار الأساسي، ثم تُعرض الخيارات الفرعية الإلزامية ويمنع التحقق الإضافة أو المتابعة قبل اختيار أحدها. ظهرت تفاصيل المنتج بعد زري تأكيد الشراء وإضافة السلة.

تمت معاينة نموذج الباقات والخدمات في الأدمن. الحقول الجديدة للخيار الأساسي والخيار الفرعي والسعر مضافة داخل النموذج، وهي متجاوبة، لكن النموذج طويل ويحتاج تمريرًا للوصول إلى محرر الخيارات، وهو سلوك متوقع في نموذج الإدارة المحمول.
