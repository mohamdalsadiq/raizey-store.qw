{
  "name": "digital-store",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx --yes serve . -p 3000",
    "test": "node tests/security-hardening.test.js",
    "build": "mkdir -p dist && cp -r assets docs tests *.html *.js *.json *.sql dist/",
    "start": "npx --yes serve . -p 3000"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
